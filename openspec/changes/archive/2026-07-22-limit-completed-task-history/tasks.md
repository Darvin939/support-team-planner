## 1. Хранение даты завершения

- [x] 1.1 Добавить идемпотентную миграцию `tasks.completed_at`, индекс `(team_id, completed_at)` и backfill последнего достоверного терминального перехода из `task_history`
- [x] 1.2 Обновить смену статуса так, чтобы `task_status` и `completed_at` изменялись атомарно по правилам жизненного цикла
- [x] 1.3 Добавить автоматические тесты миграции, backfill и установки/очистки/сохранения `completed_at`

## 2. Прямое получение работы

- [x] 2.1 Добавить DAO и защищённый `GET /api/task/{task_id}`, возвращающий любую доступную неудалённую работу независимо от статуса
- [x] 2.2 Перевести `useTaskById` и соответствующий query key на отдельный endpoint
- [x] 2.3 Покрыть endpoint тестами успешного получения старой терминальной работы, 404 и контроля доступа

## 3. Актуальный список планирования

- [x] 3.1 Заменить `show_completed` в DAO и `/api/tasks/{team_id}` на согласованную выборку активных плюс опциональных терминальных работ с `completed_at` за последние 30 дней
- [x] 3.2 Обновить frontend hook/query key и переименовать переключатель в «Завершённые за 30 дней» со сбросом пагинации
- [x] 3.3 Добавить тесты границы 30 дней, исключения `completed_at=NULL`, поиска, пагинации и согласованности `tasks/total`

## 4. Архив работ

- [x] 4.1 Реализовать архивные DAO-запросы элементов и count с терминальными статусами, нечётким поиском, включительными границами периода и стабильной сортировкой
- [x] 4.2 Добавить защищённый `GET /api/tasks/{team_id}/archive` с валидацией пагинации и диапазона дат
- [x] 4.3 Добавить отдельные frontend query keys/hooks и интерфейс архива с поиском, диапазоном завершения и offset-pagination
- [x] 4.4 Покрыть архив автоматическими DAO/API-тестами, включая недатированные legacy-работы и контроль доступа

## 5. Интеграционная проверка

- [x] 5.1 Выполнить релевантные backend-тесты и production-сборку frontend
- [x] 5.2 Перед UI-тестом создать WAL-safe backup БД и проверить через Python + Playwright вход, переключатель недавних завершённых, архив и прямое открытие старой работы
- [x] 5.3 Остановить запущенный тестом backend, восстановить изменённую БД через SQLite backup API с `wal_checkpoint(TRUNCATE)` и проверить успешность восстановления
- [x] 5.4 Выполнить полный доступный набор автоматических проверок и зафиксировать результаты

## 6. Восстановление работы из архива

- [x] 6.1 Реализовать транзакционную DAO-операцию восстановления `done/cancelled → new` с очисткой `completed_at`, записью истории и сохранением зависимостей и назначений
- [x] 6.2 Добавить защищённый `POST /api/task/{task_id}/restore` для editor/admin с проверкой доступа, 404 для отсутствующей или удалённой работы и отклонением активной работы
- [x] 6.3 Добавить в архивную модалку доступное editor/admin действие «Восстановить» с подтверждением, состоянием загрузки и инвалидацией основного списка, архива и прямой выборки
- [x] 6.4 Покрыть восстановление DAO/API-тестами успешных ролей, запрета для user, повторного вызова и сохранности зависимостей и назначений
- [x] 6.5 Собрать production frontend и выполнить Python + Playwright UI-тест восстановления с WAL-safe backup/restore БД и обязательной остановкой backend
- [x] 6.6 Выполнить полный набор backend-тестов, проверить OpenSpec и отсутствие ошибок diff

## 7. Единое оформление архивных статусов

- [x] 7.1 Заменить стандартный `Tag` статуса в архиве на общий `tintedStyle` с theme success/error цветами, собрать frontend и проверить оба терминальных статуса в production UI
