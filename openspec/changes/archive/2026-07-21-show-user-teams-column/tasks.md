## 1. Отображение команд

- [x] 1.1 Добавить в `UsersTab` сопоставление `team_ids` с названиями из справочника команд и константу лимита `N = 3`.
- [x] 1.2 Добавить колонку «Команды» с состоянием «Все команды», тегами первых трёх команд, индикатором `+N` и подсказкой полного списка.
- [x] 1.3 Ограничить ширину колонки и настроить перенос содержимого, обновив горизонтальный scroll таблицы при необходимости.

## 2. Проверка

- [x] 2.1 Выполнить TypeScript-проверку и production-сборку frontend.
- [x] 2.2 Создать WAL-безопасную резервную копию БД и проверить через Python + Playwright сценарии для 0, 1–3 и более 3 выбранных команд, включая полный список в tooltip.
- [x] 2.3 Остановить запущенный тестом backend и восстановить БД через SQLite backup API с `wal_checkpoint(TRUNCATE)`, если тест менял данные.
