## 1. Общая инфраструктура

- [x] 1.1 Создать `AppPagination` с едиными отступами, скрытием одной страницы, явными `allowPageSizeChange` и `compact` и нормализацией недопустимой текущей страницы
- [x] 1.2 Актуализировать `usePaginationState` как единственный page-based state owner и удалить offset-based setter после миграции потребителей
- [x] 1.3 Добавить unit/component-тесты преобразования `page/pageSize → offset`, смены размера, фиксированного режима, compact-вида, скрытия и нормализации по `total`

## 2. Основные серверные списки

- [x] 2.1 Перевести планировщик и журнал на `AppPagination` с изменяемым размером страницы
- [x] 2.2 Перевести пользователей и команды с прямого Ant Design `Pagination` на `AppPagination`
- [x] 2.3 Упростить обе секции статистики до page-based контракта, сохранив независимые страницы и общий синхронный `pageSize`
- [x] 2.4 Дополнить component-тесты основных списков проверками смены страницы, размера и сброса при фильтрах

## 3. Фиксированные и компактные списки

- [x] 3.1 Перевести историю в журнале, `HistoryPanel` и архив работ с `OffsetPagination` на общий hook и `AppPagination` с фиксированным размером
- [x] 3.2 Перевести Drawer новых работ и просроченных назначений с ручного `useState(page)` на `usePaginationState` и скрыть неподдерживаемую смену размера
- [x] 3.3 Перевести клиентские справочники блоков с локальной пагинации на общий hook и `AppPagination`
- [x] 3.4 Дополнить component-тесты Drawer, истории, архива и локальных справочников проверками фиксированного/compact поведения и сбросов контекста

## 4. Удаление дублирования и проверка

- [x] 4.1 Удалить `OffsetPagination`, прежний `PagePagination`, локальные обёртки и прямые импорты Ant Design `Pagination`; проверить отсутствие ручных преобразований page/offset в UI
- [x] 4.2 Запустить весь Vitest-набор и production-сборку frontend
- [x] 4.3 Создать WAL-безопасную резервную копию БД и выполнить production Playwright-проверку desktop/mobile: основные таблицы, compact Drawer/история, смена pageSize и нормализация после уменьшения total; остановить backend и восстановить БД при изменениях
