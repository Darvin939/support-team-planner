## 1. SQLite-хранение

- [x] 1.1 Добавить nullable-колонку `tasks.instruction_url` в свежую SQLite-схему и идемпотентный migration-step для существующих баз.
- [x] 1.2 Добавить migration-тесты для новой колонки, повторного запуска, `NULL` у старых строк и сохранности `description` со ссылкой.

## 2. DAO и API

- [x] 2.1 Включить `instruction_url` в SELECT/INSERT/UPDATE работ, create-snapshot и field-history, не изменяя атомарность сохранения работы и зависимостей.
- [x] 2.2 Обновить `TaskIn`, `TaskOut` и `_task_json`, нормализовать пустое значение в `None` и отклонять URL не HTTP(S), без hostname или длиннее 2048 символов.
- [x] 2.3 Добавить backend-тесты API-контракта, create/update/clear, истории, валидации и отката составной операции при ошибке.

## 3. Frontend

- [x] 3.1 Добавить `instruction_url` в доменный `Task`, task payload и локализованное форматирование истории.
- [x] 3.2 Добавить после `Описание` полноширинное поле `Ссылка на инструкцию` с frontend-валидацией и единым submit.
- [x] 3.3 Показывать `instruction_url` в read-only-модалке как безопасную ссылку в новой вкладке, сохранив `linkify` для описания.
- [x] 3.4 Расширить component-тесты `TaskModal`: начальное значение, submit, frontend-ошибка, read-only-ссылка и одновременная кликабельность URL в описании.
- [x] 3.5 Показывать в колонке `Работа` компактную ссылку `Инструкция ↗` только при непустом `instruction_url`, предотвращая открытие модалки и контекстного меню по клику на ссылку.
- [x] 3.6 Добавить component-тест планировщика для условного вывода, безопасных атрибутов и изоляции click/context-menu ссылки от действий строки.

## 4. Проверка

- [x] 4.1 Запустить целевые backend/frontend-тесты, полные backend и frontend test suites, lint и production build.
- [x] 4.2 Создать WAL-безопасную резервную копию `database.db` и проверить production UI через Python + Playwright: ввод/валидацию/сохранение ссылки, ссылку `Инструкция ↗` в планировщике, отдельное read-only-представление и ссылки в описании; затем остановить backend и восстановить БД через SQLite backup API.

## 5. Завершение OpenSpec

- [x] 5.1 Синхронизировать delta-specs `task-management` и `task-modal-layout` с основными спецификациями после отдельного разрешения пользователя.
- [x] 5.2 Архивировать завершённое изменение после отдельного разрешения пользователя.
