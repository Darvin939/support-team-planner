## 1. Pydantic-контракты и routes

- [x] 1.1 Добавить `PriorityPosition = Literal['start', 'end']` и применить его к `TaskPriorityIn.position`.
- [x] 1.2 Удалить ручную проверку критичности в task route и `_VALID_ROLES` с проверками в user routes.
- [x] 1.3 Удалить ручную проверку позиции приоритета после её типизации и сохранить бизнес-проверки доступа/состояния.
- [x] 1.4 Обновить API/OpenAPI-тесты допустимых значений и единого отклонения неизвестных enum через HTTP 422.

## 2. Терминальные статусы runtime

- [x] 2.1 Ввести в `task_rules.py` общий неизменяемый terminal set и типизированный helper без дублирования карты переходов.
- [x] 2.2 Заменить локальные Python tuple-проверки `done`/`cancelled` в business rules и DAO общим helper/набором.
- [x] 2.3 Централизовать безопасно параметризуемые runtime SQL-фильтры терминальных статусов; исторические migration steps оставить неизменными.
- [x] 2.4 Добавить контрактные тесты согласованности `TaskStatus`, terminal set и `taskTransitions.json`, а также отсутствия дублирующих route-наборов.

## 3. Проверка и завершение

- [x] 3.1 Запустить целевые и полные backend-тесты и статические проверки проекта.
- [x] 3.2 После отдельного разрешения синхронизировать новую capability с основными спецификациями.
- [x] 3.3 После отдельного разрешения архивировать изменение.
- [x] 3.4 После отдельного разрешения создать commit.
