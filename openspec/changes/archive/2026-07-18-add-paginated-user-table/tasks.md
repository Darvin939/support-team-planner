## 1. Backend-поиск и пагинация

- [x] 1.1 Добавить DAO-выборку пользователей с `offset`, `limit`, общим `total` и поиском по логину, ФИО и роли.
- [x] 1.2 Расширить `GET /api/users` пагинированным режимом `{ users, total }`, сохранив ответ-массив без query-параметров.
- [x] 1.3 Добавить backend-тесты пагинации, поиска по логину/ФИО/роли и обратной совместимости полного списка.

## 2. Frontend-таблица

- [x] 2.1 Вынести общие `DEFAULT_PAGE_SIZE` и `PAGE_SIZE_OPTIONS` и перевести на них Planning и Statistics.
- [x] 2.2 Добавить типизированный `usePaginatedUsers` и состояния поиска, debounced search, страницы и размера страницы в `UsersTab`.
- [x] 2.3 Заменить `List` на `Table` с колонками «ФИО», «Роль», «Логин», «Действия», сохранив защищённую учётную запись и role-based actions.
- [x] 2.4 Добавить `Input.Search` и отдельный `Pagination` с общими размерами страницы и корректными сбросами на первую/предыдущую страницу.

## 3. Проверка

- [x] 3.1 Запустить backend-тесты и production-сборку frontend через `npm run build` из `frontend`.
- [ ] 3.2 Создать backup `database.db`, запустить production-backend на 5093, войти через реальную `/login`-форму под `admin` / `q12345678` и проверить таблицу, серверный поиск, пагинацию и действия через Python + Playwright; остановить запущенные тестом Python-процессы и восстановить БД, если тест её изменял.
