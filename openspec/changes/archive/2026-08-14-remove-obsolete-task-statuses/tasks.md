## 1. Backend-контракты перечислений

- [x] 1.1 Ввести переиспользуемые `Literal`-алиасы текущего статуса работы, статуса назначения, критичности и роли и применить их к соответствующим Pydantic input/output-моделям.
- [x] 1.2 Сохранить дополнительные route-проверки переходов/доступа, строго типизировать текущий статус в read-моделях и оставить строковыми только общие значения аудита.
- [x] 1.3 Добавить API/model-тесты всех допустимых наборов и отклонения неизвестного статуса работы, статуса назначения, критичности и роли до записи в БД.

## 2. Demo seed-данные

- [x] 2.1 Удалить `ready`/`in_progress` из `seed_demo_data.py`, включая специальный сценарий, который явно назначает `in_progress`.
- [x] 2.2 Удалить `ready`/`in_progress` из `seed_large_demo_data.py` и сохранить репрезентативное распределение трёх актуальных статусов.
- [x] 2.3 Добавить изолированные проверки, что оба штатных seed-источника больше не создают `ready`/`in_progress`.
- [x] 2.4 Создать WAL-safe backup и разово заменить `ready`/`in_progress` на `new` в текущей seeded-БД и значениях истории статуса; проверить отсутствие legacy-кодов и целостность.

## 3. Frontend-словарь и планировщик

- [x] 3.1 Сузить `TaskStatus` и `TASK_STATUS_OPTIONS` до `new`, `done`, `cancelled`; типизировать status mutation, criticality и связанные props актуальными union-типами там, где неизвестные значения не требуются.
- [x] 3.2 Типизировать options/labels статуса работы, назначения, критичности, ПСИ и роли без compatibility-карт или helper-функций для `ready`/`in_progress`.
- [x] 3.3 Заменить локальную карту статусов назначения в статистике, inline-карту ПСИ в истории и дублирующий порядок критичности централизованными источниками.
- [x] 3.4 Обновить fixtures и добавить Vitest/component-тесты фильтра и централизованных актуальных подписей без legacy-ожиданий.
- [x] 3.5 Добавить контрактную проверку согласованности актуальных options/labels с union-типами и `taskTransitions.json`.

## 4. Проверка и завершение

- [x] 4.1 Запустить целевые и полные backend/frontend test suites, lint и production build.
- [x] 4.2 Создать WAL-безопасную резервную копию БД и проверить production UI через Python + Playwright: фильтр содержит только три актуальных статуса, подписи назначения/ПСИ отображаются централизованно, архивные статусы работают как прежде; затем остановить backend и восстановить БД через SQLite backup API.
- [x] 4.3 После отдельного разрешения синхронизировать delta-specs с основными спецификациями.
- [x] 4.4 После отдельного разрешения архивировать завершённое изменение.
