## 1. Объединённая навигация и layout

- [x] 1.1 Заменить вкладки `blocks` и `segments` одной вкладкой `references` с названием «Справочники работ».
- [x] 1.2 Нормализовать сохранённые ключи `blocks`/`segments` к `references`, а неизвестный ключ — к безопасной вкладке по умолчанию.
- [x] 1.3 Создать адаптивный двухколоночный layout 9/15 с вертикальным стеком на узких экранах.

## 2. Таблицы справочников

- [x] 2.1 Реализовать переиспользуемую клиентскую фильтрацию и пагинацию по 5 строк с независимым состоянием каждой панели.
- [x] 2.2 Переделать сегменты в таблицу с inline-toolbar, поиском, пагинацией и существующими действиями.
- [x] 2.3 Переделать блоки в таблицу с inline-toolbar, поиском, пагинацией, modal-созданием и существующим удалением.
- [x] 2.4 Переделать шаблоны в таблицу с названием, сегментом, компактным составом до трёх тегов, `+N`/tooltip и существующими действиями.
- [x] 2.5 Сохранить формы, query invalidation, подтверждения удаления и отображение backend-ошибок всех трёх справочников.
- [x] 2.6 Удалить устаревшую отдельную вкладку/компонент сегментов после переноса поведения.

## 3. Проверка

- [x] 3.1 Выполнить TypeScript-проверку и production-сборку frontend.
- [x] 3.2 Создать WAL-безопасный backup и проверить через Python + Playwright объединённую вкладку, desktop/mobile layout, независимый поиск, пагинацию, компактный состав и открытие CRUD-форм.
- [x] 3.3 Остановить запущенный тестом backend, восстановить изменённую БД через SQLite backup API и выполнить `wal_checkpoint(TRUNCATE)` с проверкой целостности.
