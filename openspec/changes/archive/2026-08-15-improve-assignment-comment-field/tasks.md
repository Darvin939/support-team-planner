## 1. Поле и отображение комментария

- [x] 1.1 Заменить `Input` комментария в `AssignmentModal` на `Input.TextArea` с `autoSize` от 1 до 3 строк.
- [x] 1.2 Ограничить комментарий в `ScheduleChip` тремя строками с line-clamp, шириной карточки и полным текстом в `title`.
- [x] 1.3 Убедиться, что пустой комментарий не резервирует место и сохранение payload не изменилось.

## 2. Автоматические проверки

- [x] 2.1 Добавить или обновить Vitest + React Testing Library component-тесты TextArea и компактного комментария.
- [x] 2.2 Запустить целевые и полные frontend-тесты, lint и production build.
- [x] 2.3 Создать WAL-safe backup и проверить production UI через Python + Playwright; остановить backend и восстановить БД через SQLite backup API.

## 3. Завершение OpenSpec

- [x] 3.1 После отдельного разрешения синхронизировать новую capability с основными спецификациями.
- [x] 3.2 После отдельного разрешения архивировать изменение.
- [x] 3.3 После отдельного разрешения создать commit.
