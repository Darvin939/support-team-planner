## 1. Пустое состояние графа

- [x] 1.1 Добавить в `DependencyGraphModal` признак пустого графа для конкретной работы без рёбер, не меняя критерий пустоты общего графа.
- [x] 1.2 Показать для изолированной работы `Empty` с сообщением «У работы нет зависимостей».

## 2. Проверка

- [x] 2.1 Выполнить production-сборку frontend командой `npm run build` из каталога `frontend`.
- [x] 2.2 Перед UI-тестом создать backup `database.db`, запустить production-backend на порту 5093 и через Python + Playwright проверить `Empty` для работы без рёбер, обычный граф для работы с ребром и прежнее поведение общего графа; в `finally` остановить все запущенные тестом Python-процессы.
