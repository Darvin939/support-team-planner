## 1. Модель данных и вычисление доступа

- [x] 1.1 Добавить идемпотентную схему `user_team_access` с внешними ключами и каскадным удалением.
- [x] 1.2 Реализовать DAO получения/замены разрешённых команд и вычисления эффективного доступа с правилами «admin — все» и «нет строк — все».
- [x] 1.3 Добавить DAO-проверки команды по `team_id`, `task_id`, `assignment_id` и выборку допустимых исполнителей команды.

## 2. Настройки пользователя

- [x] 2.1 Расширить модели и пользовательский CRUD API полем `team_ids`, сохраняя пользователя и связи в одной транзакции.
- [x] 2.2 Возвращать `team_ids` в полном и пагинированном списках пользователей, не изменяя их существующие форматы ответа.
- [x] 2.3 Добавить в форму пользователя множественный выбор команд, пояснение пустого значения и корректное поведение для администратора.

## 3. Backend-авторизация командных данных

- [x] 3.1 Добавить общие FastAPI helpers проверки одного/нескольких team ID и команды связанных задач/назначений.
- [x] 3.2 Фильтровать `/api/teams` по текущему пользователю и защитить чтение, изменение и удаление существующих команд; при создании команды ограниченным редактором выдать ему доступ.
- [x] 3.3 Защитить endpoints задач, зависимостей, графа и перестановки от прямого и косвенного доступа к чужой команде.
- [x] 3.4 Защитить чтение, создание, изменение, перенос и удаление назначений, включая проверку допустимости выбранного исполнителя.
- [x] 3.5 Ограничить журнал и статистику доступными командами; отклонять явно переданные недоступные ID без частичного результата.

## 4. Контекстный список исполнителей и frontend

- [x] 4.1 Добавить `/api/teams/{team_id}/assignees` и перевести AssignmentModal на контекстную выборку исполнителей.
- [x] 4.2 Обработать сохранённые в localStorage недоступные команды в Planning и Journal и исключить их из фильтров Statistics.
- [x] 4.3 Проверить role-based UI для администратора, ограниченного редактора и ограниченного пользователя.

## 5. Автоматические проверки

- [x] 5.1 Добавить backend-тесты миграции, полного/ограниченного/admin-доступа и фильтрации `/api/teams`.
- [x] 5.2 Добавить backend-тесты `403` для прямых и косвенных обращений к задачам, зависимостей, назначений, статистике и журналу.
- [x] 5.3 Добавить тесты списка исполнителей и запрета прямого назначения пользователя без доступа к команде.
- [x] 5.4 Запустить backend-тесты и production-сборку frontend через `npm run build`.
- [x] 5.5 Сделать WAL-безопасный backup БД и выполнить production UI-тесты Python + Playwright с реальной авторизацией под admin/editor/user; остановить процессы и восстановить БД.
