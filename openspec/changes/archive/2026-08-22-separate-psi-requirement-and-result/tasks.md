## 1. Серверное изменение результата ПСИ

- [x] 1.1 Добавить входную модель и узкую API-операцию перехода активной работы между `required` и `passed` с существующими проверками доступа, терминального статуса и назначений
- [x] 1.2 Переиспользовать общую валидацию перехода ПСИ и сохранить автора изменения в истории работы
- [x] 1.3 Добавить backend-тесты успешных переходов, отказа для терминальной работы и отказа `passed` → `required` при наличии назначений

## 2. Чекбокс требования в форме работы

- [x] 2.1 Заменить Select ПСИ в `TaskModal` на чекбокс «ПСИ требуется» без изменения readonly-отображения
- [x] 2.2 Реализовать преобразование checkbox-to-enum: `not_required` → `required`, сохранение исходных `required`/`passed` при включённом чекбоксе и перевод в `not_required` при выключенном
- [x] 2.3 Дополнить component-тесты `TaskModal` всеми вариантами преобразования и защитой `passed` от случайного сброса

## 3. Контекстное меню планировщика

- [x] 3.1 Добавить клиентскую мутацию результата ПСИ с корректной обработкой ошибки и инвалидацией затронутых запросов
- [x] 3.2 Добавить для активных работ пункты `required` → `passed` и `passed` → `required`, каждый с подтверждением; не показывать действие для `not_required`
- [x] 3.3 Дополнить component-тесты колонок проверками состава меню, подтверждения, отмены и вызова мутации для обоих переходов

## 4. Проверка

- [x] 4.1 Запустить целевые backend- и frontend-тесты, затем полный релевантный набор проверок и production-сборку frontend
- [x] 4.2 Создать WAL-безопасную резервную копию БД и выполнить production Playwright-сценарий: чекбокс в модалке, подтверждаемые переходы через контекстное меню, неизменные бейджи и серверный отказ при назначениях; остановить backend и восстановить БД при изменениях

## 5. Блокировка требования при назначениях

- [x] 5.1 Расширить backend-валидацию полного обновления работы двусторонним запретом изменения требования ПСИ при любых неудалённых назначениях
- [x] 5.2 Заблокировать checkbox «ПСИ требуется» в `TaskModal` по признаку наличия назначений и гарантировать отправку исходного `psi_status`
- [x] 5.3 Добавить backend- и component-тесты запрета отключения требования при назначениях, блокировки checkbox для всех состояний и сохранения исходного значения
- [x] 5.4 Повторить production-сборку и Playwright-проверку заблокированного checkbox с восстановлением БД
