## 1. Согласованная загрузка таблицы

- [x] 1.1 Получать из hooks состояния первоначальной загрузки и ошибок списка работ, назначений, зависимостей и нерабочих дней.
- [x] 1.2 Вычислить readiness текущей страницы с отдельной обработкой пустого списка работ и disabled зависимых queries.
- [x] 1.3 Показывать `Spin` до готовности, `Empty` для успешно загруженной пустой страницы и состояние ошибки без частичной таблицы.

## 2. Центрирование

- [x] 2.1 Привязать `scrollGridToToday` к готовности полностью смонтированного planning grid и не сбрасывать ожидание до успешного позиционирования.
- [x] 2.2 Сохранить повторное центрирование при смене команды, периода, поиска, фильтра завершённых работ и страницы без мерцания при фоновых refetch.

## 3. Проверка

- [x] 3.1 Собрать production-frontend и устранить ошибки TypeScript/build.
- [x] 3.2 Перед UI-тестом создать WAL-безопасную резервную копию `database.db`, затем проверить через Python + Playwright реальную авторизацию, отсутствие промежуточного planning grid и центрирование на сегодняшней дате после загрузки.
- [x] 3.3 Остановить запущенный тестом backend; если тест изменил БД, восстановить её через SQLite backup API и выполнить `PRAGMA wal_checkpoint(TRUNCATE)`.
