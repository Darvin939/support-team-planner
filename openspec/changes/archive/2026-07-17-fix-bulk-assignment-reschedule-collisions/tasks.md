## 1. Серверный контракт и проверка

- [x] 1.1 Добавить модели запроса массового переноса и синхронный API-маршрут, принимающий идентификаторы назначений с итоговыми датами и возвращающий при ошибке стандартный JSON-формат.
- [x] 1.2 Загружать актуальное состояние всех переданных назначений и задач; до изменения данных отклонять пустой или слишком большой запрос, повторяющиеся и отсутствующие идентификаторы, некорректные или выходящие за диапазон даты, заблокированные задачи и нарушения ограничений роли или статуса.
- [x] 1.3 Проверять полную итоговую раскладку: отклонять повторяющиеся целевые пары `(task_id, date)` и назначения вне переносимой группы в целевых ячейках, разрешая ячейки, освобождаемые самой группой.

## 2. Атомарное сохранение и аудит

- [x] 2.1 Добавить DAO-операцию массового переноса, которая временно размещает перемещаемые строки на уникальных служебных датах и записывает все итоговые даты в существующей единой транзакции без промежуточных commit.
- [x] 2.2 Записывать для каждого изменённого назначения одну логическую запись аудита от исходной даты к итоговой с `changed_by=request.session.get('user_id')`, не включая служебные даты в историю.
- [x] 2.3 Обеспечить откат всех изменений дат и истории при любой ошибке проверки или сохранения и возвращать русскоязычную ошибку API, не меняя существующий маршрут одиночного назначения.

## 3. Проверка итогового состояния при перетаскивании

- [x] 3.1 Изменить callback проверки занятости на странице планирования так, чтобы он возвращал данные назначения, занимающего целевую ячейку.
- [x] 3.2 Обновить `useAssignmentDrag.ts`: вычислять все цели по неизменяемому снимку выделения, разрешать исходные ячейки выбранных назначений и отклонять внешние назначения, повторяющиеся итоговые пары и даты вне диапазона.
- [x] 3.3 Согласовать допустимую и недопустимую подсветку ячеек и запрет drop с результатом групповой проверки, включая восстановление рамок выбранных ячеек после завершения перетаскивания.

## 4. Массовый запрос и обратная связь

- [x] 4.1 Заменить цикл последовательных POST-запросов для нескольких назначений одним вызовом нового атомарного маршрута, сохранив прежнее поведение одиночного переноса.
- [x] 4.2 При успехе обновлять запросы назначений и активных назначений и показывать количество перенесённых элементов; при ошибке обновлять данные и показывать русскоязычное сообщение без частичного успеха.

## 5. Проверка результата

- [x] 5.1 Выполнить smoke-проверки успешных цепочек в одной строке (день 1 переносится в освобождаемый день 3), перестановок и выборок из нескольких задач; проверить итоговые даты и записи аудита.
- [x] 5.2 Выполнить smoke-проверки внешних коллизий, повторяющихся целей, отсутствующих идентификаторов, назначений в продвинутом статусе для роли `user`, конкурентного изменения состояния и принудительного отката без частичных изменений.
- [x] 5.3 Проверить подсветку перетаскивания для освобождаемых ячеек и каждого недопустимого случая, затем выполнить `npm run lint` и `npm run build` в `frontend/`.
