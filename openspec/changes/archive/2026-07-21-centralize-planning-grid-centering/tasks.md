## 1. Централизованный hook

- [x] 1.1 Создать `usePlanningGridTodayCenter`, инкапсулирующий pending-ref, отслеживание ключа представления и DOM-центрирование после готовности grid.
- [x] 1.2 Сформировать стабильный ключ представления из серверных параметров, всех клиентских фильтров и отсортированной сигнатуры состава работ.
- [x] 1.3 Подключить hook в `PlanningPage` и удалить `scrollGridToToday`, `pendingCenterRef`, ручной callback и прямые присваивания из обработчиков и mutations.

## 2. Проверка поведения

- [x] 2.1 Собрать production-frontend и устранить ошибки TypeScript/build.
- [x] 2.2 Перед UI-тестом создать WAL-безопасную резервную копию `database.db` и через Python + Playwright проверить центрирование после серверного и каждого клиентского фильтра.
- [x] 2.3 В UI-тесте проверить сохранение ручной горизонтальной позиции при refetch/изменении назначения и reorder без изменения состава работ.
- [x] 2.4 Остановить запущенный тестом backend; при изменении БД восстановить её через SQLite backup API, выполнить `PRAGMA wal_checkpoint(TRUNCATE)` и проверить целостность.
