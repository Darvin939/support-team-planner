## 1. Backend списка команд

- [x] 1.1 Добавить DAO пагинированного списка команд с поиском, `total`, загрузкой шаблонов только для страницы и применением эффективного командного доступа до фильтрации.
- [x] 1.2 Расширить `GET /api/teams` опциональными `offset`, `limit`, `search`, сохранив полный массив без параметров.
- [x] 1.3 Добавить backend-тесты полного совместимого ответа, поиска, пагинации и ограничения доступа.

## 2. Таблица команд

- [x] 2.1 Добавить paginated query key, тип ответа и `usePaginatedTeams`, не изменяя контракт `useTeams`.
- [x] 2.2 Переделать `TeamsTab` с карточек на Ant Design `Table` с колонками названия, шаблонов и действий.
- [x] 2.3 Добавить debounce-поиск, пагинацию, выбор размера страницы, сброс страницы при поиске и возврат после удаления последней строки.
- [x] 2.4 Сохранить существующую форму и CRUD-инвалидацию полного и пагинированного списков.
- [x] 2.5 Объединить кнопку добавления и поиск в адаптивную inline-панель с нижним отступом на вкладках команд и пользователей.

## 3. Проверка

- [x] 3.1 Запустить релевантные backend-тесты.
- [x] 3.2 Выполнить TypeScript-проверку и production-сборку frontend.
- [x] 3.3 Создать WAL-безопасный backup и проверить через Python + Playwright таблицу, поиск, пагинацию, шаблоны и открытие действий в production-backend.
- [x] 3.4 Остановить запущенный тестом backend, восстановить изменённую БД через SQLite backup API, выполнить `wal_checkpoint(TRUNCATE)` и проверить восстановление.
- [x] 3.5 Повторить production-сборку и Playwright-проверку расположения панелей команд и пользователей на широком и узком экранах.
