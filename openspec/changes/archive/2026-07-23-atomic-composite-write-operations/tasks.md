## 1. Транзакционная инфраструктура

- [x] 1.1 Добавить в DB-слой явный контекстный менеджер составной транзакции, использующий request-scoped
  соединение или собственное соединение вне HTTP-запроса.
- [x] 1.2 Изменить `with_db_connection`, чтобы он сохранял текущий самостоятельный commit по умолчанию и
  откладывал commit внутри активной составной транзакции.
- [x] 1.3 Обеспечить один итоговый commit при успехе, rollback при исключении и явный отказ от неподдерживаемого
  вложенного transaction scope.

## 2. Атомарные бизнес-операции

- [x] 2.1 Перевести сохранение задачи, истории и переданного набора зависимостей на одну составную транзакцию;
  ошибки проверки зависимостей после записи задачи должны возбуждать исключение и приводить к rollback.
- [x] 2.2 Перевести создание команды, привязку шаблонов и выдачу ограниченного доступа создателю на одну составную
  транзакцию.
- [x] 2.3 Сохранить существующие HTTP-маршруты, успешные ответы и пользовательские сообщения об ошибках.

## 3. Backend-тесты

- [x] 3.1 Добавить unit-тесты transaction scope: один commit при успехе, rollback при исключении, отсутствие
  промежуточных commit и неизменное поведение одиночного DAO-вызова.
- [x] 3.2 Добавить тест rollback создания и редактирования задачи при ошибке обработки зависимостей, включая
  проверку истории и исходного набора зависимостей.
- [x] 3.3 Добавить тест rollback команды и связей с шаблонами при ошибке выдачи доступа.
- [x] 3.4 Запустить релевантные backend-тесты и полный набор `pytest`, затем устранить найденные регрессии.

## 4. Проверка OpenSpec

- [x] 4.1 Сверить реализацию с требованиями `composite-write-atomicity` и `dao-commit-consistency`.
- [x] 4.2 Выполнить финальную валидацию OpenSpec change перед синхронизацией спецификаций.
