## 1. Component-поведение

- [x] 1.1 Добавить component-тесты `TaskModal` для начальной вкладки, переключения вкладок и счётчика выбранных зависимостей.
- [x] 1.2 Добавить component-тест сохранения выбора при поиске и переключении вкладок, а также единого submit основных полей и `dependency_ids`.

## 2. Переработка модального окна

- [x] 2.1 Добавить в `TaskModal` управляемые вкладки `Основное` и `Зависимости`, сброс на первую вкладку при открытии и динамический счётчик `depIds.size`.
- [x] 2.2 Перенести поиск и список выбора на вкладку зависимостей, сохранив дебаунс, сортировку и единую операцию сохранения.
- [x] 2.3 Увеличить desktop-ширину модалки и её ширину с открытой историей, сохранив mobile-размер без горизонтального переполнения.
- [x] 2.4 Сделать адаптивную двухколоночную сетку коротких полей и увеличить полноширинное поле `Описание` до не менее семи видимых строк.

## 3. Проверка

- [x] 3.1 Запустить целевые Vitest-тесты `TaskModal` и полный frontend test suite.
- [x] 3.2 Создать WAL-безопасную резервную копию `database.db`, собрать production-frontend и проверить через Python + Playwright desktop/mobile-компоновку, вкладки, счётчик, большое поле описания и панель истории; остановить запущенный backend и восстановить БД через SQLite backup API, если сценарий её изменил.

## 4. Завершение OpenSpec

- [x] 4.1 Синхронизировать delta-spec `task-modal-layout` с основными спецификациями после отдельного разрешения пользователя.
- [x] 4.2 Архивировать завершённое изменение после отдельного разрешения пользователя.
