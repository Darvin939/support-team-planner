## 1. Схема и миграция состояния уведомлений

- [x] 1.1 Добавить в текущую SQLite-схему таблицу `user_notification_state` с персональным составным курсором `(changed_at, history_id)`, внешним ключом и индекс create-событий `task_history`.
- [x] 1.2 Реализовать идемпотентную миграцию, которая инициализирует существующих пользователей текущим серверным курсором и не превращает исторические работы в новые.
- [x] 1.3 Инициализировать состояние при создании нового пользователя и добавить безопасный fallback для отсутствующей строки.
- [x] 1.4 Покрыть миграцию, повторный запуск и создание пользователя backend-тестами.

## 2. Backend выборки и подтверждения

- [x] 2.1 Реализовать DAO-выборку новых работ с проверкой доступа к командам, исключением автора и удалённых работ, детерминированным курсорным порядком, limit/offset и точным count.
- [x] 2.2 Реализовать получение зафиксированного серверного watermark и монотонное подтверждение просмотра не дальше переданного курсора.
- [x] 2.3 Добавить типизированные FastAPI endpoints и модели для preview, пагинируемого полного списка и действия «Отметить все просмотренными».
- [x] 2.4 Покрыть API-тестами роли и доступ, автора, удаление, терминальные статусы, лимит 30, пагинацию, точный count, гонку create после watermark и запоздалое подтверждение.
- [x] 2.5 Проверить запрос на больших demo-данных и убедиться, что preview не загружает полную выборку.

## 3. Frontend данные и общий центр уведомлений

- [x] 3.1 Добавить TypeScript-контракты, query keys, hooks preview/списка и mutation подтверждения с согласованной инвалидацией cache.
- [x] 3.2 Преобразовать `OverdueNotifications` в общий центр с раздельными разделами новых работ и просроченных назначений, локальными loading/error/empty состояниями и суммарным badge.
- [x] 3.3 Отобразить ограниченный список новых работ сгруппированным по командам с точным общим количеством, критичностью, автором и временем создания.
- [x] 3.4 Сохранить существующее ограничение preview и переход к команде, работе и дате для просроченных назначений.
- [x] 3.5 Добавить явное подтверждение «Отметить все просмотренными» с понятным текстом при количестве сверх preview и обновлением всех связанных данных.

## 4. Обзор при входе и полный список

- [x] 4.1 Добавить неблокирующую карточку обзора при первом открытии планировщика с session-флагом показа, не связанным с курсором прочтения.
- [x] 4.2 Добавить Drawer «Все новые работы» с серверной пагинацией по фиксированному watermark и responsive-представлением.
- [x] 4.3 Реализовать переход из карточки, центра и Drawer на нужный маршрут команды через существующий `jumpTaskId`, включая работу вне текущей страницы.
- [x] 4.4 Добавить доступные подписи, клавиатурную активацию, корректные состояния пустого списка и счётчики с overflow.

## 5. Unit и component тестирование frontend

- [x] 5.1 Покрыть Vitest + React Testing Library общий центр: оба раздела, суммарный и раздельные счётчики, группировку, лимит, независимые ошибки и сохранённую навигацию просроченных назначений.
- [x] 5.2 Покрыть карточку первого входа: однократный показ за сессию, закрытие без подтверждения и отсутствие при нулевом количестве.
- [x] 5.3 Покрыть Drawer: пагинацию с неизменным watermark, переход к работе, изменение доступа и пустое состояние.
- [x] 5.4 Покрыть mutation подтверждения: передача watermark, обновление badge/списков и поведение ошибки без ложной отметки.

## 6. Production UI-проверка

- [x] 6.1 Создать WAL-безопасную резервную копию `database.db` через SQLite backup API с уникальным именем.
- [x] 6.2 После всех frontend-изменений выполнить свежую production-сборку командой `npm run build` из `frontend`.
- [x] 6.3 Через Python + Playwright и production-backend проверить реальный вход, обзор по нескольким доступным командам, общий центр, Drawer, переход к конкретной работе и явное подтверждение просмотра на desktop и узком viewport.
- [x] 6.4 В `finally` остановить только запущенный тестом backend; восстановить изменённую БД через SQLite backup API, выполнить `PRAGMA wal_checkpoint(TRUNCATE)` и проверить успешность восстановления до удаления backup.

## 7. Завершение изменения

- [x] 7.1 Выполнить полный релевантный набор backend- и frontend-тестов и зафиксировать результаты в задачах изменения.

## Результаты предыдущей проверки

- Backend: `python -m unittest discover -s tests` — 112 тестов пройдено.
- Frontend: `npm test -- --run` — 17 файлов, 48 тестов пройдено.
- Production build: `npm run build` — успешно.
- Production UI: реальная авторизация, обзор двух команд, Drawer на desktop/узком viewport, общий центр и подтверждение просмотра — успешно; тестовый backend остановлен, БД восстановлена через SQLite backup API и проверена.

## 8. Компактный общий центр

- [x] 8.1 Ограничить отрисовку вкладки новых работ первыми 5 элементами, сохранив точное количество, группировку показанного подмножества и действие «Показать все».
- [x] 8.2 Установить общую фиксированную ширину и ограниченную высоту содержимого обеих вкладок без выхода popover за viewport и без пустого правого отступа при переключении.
- [x] 8.3 Изменить preview просроченных назначений на серверный limit 5 и добавить сообщение «Показаны первые 5 из N» с кнопкой «Показать все».

## 9. Drawer просроченных назначений

- [x] 9.1 Добавить Drawer полного списка просроченных назначений с серверной пагинацией по неизменному периоду уведомлений.
- [x] 9.2 Сохранить переход из Drawer к команде, работе и дате назначения с закрытием Drawer и центра.
- [x] 9.3 Добавить loading, error, empty и responsive-состояния Drawer.

## 10. Тестирование уточнённого поведения

- [x] 10.1 Покрыть component-тестами лимит 5 для обеих вкладок, точные количества, сообщения ограничения и открытие соответствующих Drawer.
- [x] 10.2 Покрыть component-тестами серверную пагинацию и навигацию Drawer просроченных назначений.
- [x] 10.3 Выполнить свежую production-сборку и UI-тест под `demo08` / `123`: одинаковая ширина вкладок, popover в пределах viewport, по 5 элементов и пагинация обоих Drawer.
- [x] 10.4 После UI-теста остановить запущенный тестом backend и подтвердить отсутствие изменений БД либо восстановить её через WAL-безопасный backup при наличии тестовых мутаций.

## 11. Единый каркас центра уведомлений

- [x] 11.1 Вынести просроченные назначения из `OverdueNotifications` в самостоятельный компонент и оставить `OverdueNotifications` каркасом popover, вкладок и общего контекста.
- [x] 11.2 Добавить общий компонент состояния вкладки, который принимает содержимое и действия и единообразно отображает loading/error/empty, «Показаны первые N из M» и кнопки.
- [x] 11.3 Перевести новые работы и просроченные назначения на общий контракт без дублирования разметки и стилей служебной части вкладки.

## 12. Безопасная навигация

- [x] 12.1 Отложить переход из Drawer до завершения его закрытия, чтобы модальная форма назначения не открывалась под блокирующим overlay.
- [x] 12.2 Выделить общую логику фокуса работы из `handleDepNavigate` и использовать её для task-only `jumpTaskId` после загрузки команды, не меняя assignment-jump с обязательной `jumpDate`.

## 13. Тестирование рефакторинга и исправлений

- [x] 13.1 Покрыть Vitest + React Testing Library общий контракт вкладки, обе панели и отложенную навигацию после закрытия Drawer.
- [x] 13.2 Покрыть task-only jump: прокрутку и подсветку видимой строки, fallback для отсутствующей строки и сохранение поведения назначения с датой.
- [x] 13.3 Выполнить полный frontend test suite и свежую production-сборку.
- [x] 13.4 Создать WAL-безопасный backup и через Python + Playwright под `demo08` / `123` проверить вкладки, оба Drawer, отсутствие soft lock и фокус новой работы; остановить backend и при изменении БД восстановить backup.

## 14. Независимая подсветка строки

- [x] 14.1 Перенести пульсацию строки на независимый CSS overlay, не конфликтующий с `box-shadow` hover-состояния ячейки.
- [x] 14.2 Покрыть сохранение анимации при наведении тестом и выполнить frontend test suite и production build.

## 15. Устойчивая к перерисовке подсветка

- [x] 15.1 Перенести идентификатор подсвеченной работы в React state и назначать класс декларативно через таблицу, сохранив общий механизм фокуса для уведомлений и зависимостей.
- [x] 15.2 Обновить CSS overlay для класса строки и покрыть таймер, смену подсвеченной работы и сохранение класса при React rerender component-тестами.
- [x] 15.3 Выполнить frontend test suite, production build и production UI-сценарий со smooth-scroll под неподвижный курсор.

## 16. Безопасный переход из Drawer обзора

- [x] 16.1 Применить общий механизм отложенной Drawer-навигации в `NewTasksOverviewCard`, выполняя переход только после `afterOpenChange(false)`.
- [x] 16.2 Покрыть component-тестом закрытие Drawer до навигации и выполнить frontend test suite, production build и production UI-проверку работы вне текущей таблицы.

## 17. Повторный запуск и полная ширина подсветки

- [x] 17.1 Добавить revision подсветки и защиту callback таймера от очистки более нового состояния; гарантировать перезапуск анимации при повторном выборе той же работы.
- [x] 17.2 Перенести overlay на слой, не конфликтующий с фиксированной колонкой Ant Design, и покрыть подсветкой колонку работы и назначения.
- [x] 17.3 Покрыть быстрые последовательные вызовы, stale timer, повтор той же работы и классы строк unit/component-тестами; выполнить frontend suite, build и production UI.

## 18. Завершение уточнённого изменения

- [x] 18.1 Синхронизировать delta specs с основными спецификациями после отдельного разрешения пользователя.
- [x] 18.2 Архивировать завершённое изменение после отдельного разрешения пользователя.

## Результаты уточнённой проверки

- Frontend: `npm test -- --run` — 18 файлов, 49 тестов пройдено.
- Production build: `npm run build` — успешно.
- Production UI под `demo08` / `123`: обе вкладки выводят по 5 элементов, имеют одинаковую ширину 424 px и помещаются в viewport 1440×900; оба Drawer используют серверную пагинацию, включая вторую страницу.
- Запущенный тестом backend остановлен; БД восстановлена через SQLite backup API и checkpoint выполнен.

## Результаты проверки каркаса и навигации

- Frontend: `npm test -- --run` — 20 файлов, 53 теста пройдено.
- Production build: `npm run build` — успешно.
- Production UI под `demo08` / `123`: Drawer просроченных назначений полностью закрывается до открытия модалки; task-only jump прокручивает и подсвечивает строку новой работы.
- Запущенный тестом backend остановлен; БД восстановлена через SQLite backup API, checkpoint и `integrity_check` выполнены успешно.

## Результаты проверки подсветки

- Frontend: `npm test -- --run` — 20 файлов, 53 теста пройдено.
- Production build: `npm run build` — успешно.
- Production UI под `demo08` / `123`: после `mouseenter` overlay сохраняет активную анимацию `task-row-highlight-pulse` и текущую opacity, резкого исчезновения нет.
- Запущенный тестом backend остановлен; БД восстановлена через SQLite backup API, checkpoint и `integrity_check` выполнены успешно.

## Результаты проверки декларативной подсветки

- Frontend: `npm test -- --run` — 20 файлов, 54 теста пройдено.
- Production build: `npm run build` — успешно.
- Production UI под `demo08` / `123`: при настоящем hover во время подсветки строка остаётся `task-row-highlight-pulse`, а Ant Design одновременно назначает `ant-table-cell-row-hover` всем 30 ячейкам.
- Запущенный тестом backend остановлен; БД восстановлена через SQLite backup API, checkpoint и `integrity_check` выполнены успешно.

## Результаты проверки Drawer карточки обзора

- Frontend: `npm test -- --run` — 20 файлов, 55 тестов пройдено.
- Production build: `npm run build` — успешно.
- Production UI под `demo08` / `123`: активная работа другой команды исключена фильтром из таблицы; Drawer карточки обзора полностью закрывается до смены маршрута и открытия `TaskModal`.
- Запущенный тестом backend остановлен; БД восстановлена через SQLite backup API, checkpoint и `integrity_check` выполнены успешно.

## Результаты проверки повторной полной подсветки

- Frontend: `npm test -- --run` — 20 файлов, 55 тестов пройдено.
- Production build: `npm run build` — успешно.
- Production UI под `demo08` / `123`: повторный переход к той же работе переключает revision-класс `a → b` и перезапускает animation-name; overlay активен на фиксированной колонке работы и ячейках назначений.
- Запущенный тестом backend остановлен; БД восстановлена через SQLite backup API, checkpoint и `integrity_check` выполнены успешно.


